{{- if and .Values.monitoring.enabled .Values.monitoring.exporters.kafka.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-exporter
  namespace: {{ include "techbleat.namespace" . }}
  labels:
    {{- include "techbleat.labels" . | nindent 4 }}
    tier: monitoring
    app.kubernetes.io/component: kafka-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: kafka-exporter
      {{- include "techbleat.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: kafka-exporter
        tier: monitoring
        {{- include "techbleat.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "techbleat.serviceAccountName" . }}
      containers:
        - name: kafka-exporter
          image: {{ .Values.images.kafkaExporter | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          args:
            - --kafka.server=kafka:{{ .Values.kafka.service.internalPort }}
          ports:
            - name: metrics
              containerPort: {{ .Values.monitoring.exporters.kafka.port }}
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: kafka-exporter
  namespace: {{ include "techbleat.namespace" . }}
  labels:
    {{- include "techbleat.labels" . | nindent 4 }}
    tier: monitoring
    app.kubernetes.io/component: kafka-exporter
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: {{ .Values.monitoring.exporters.kafka.port }}
      targetPort: metrics
  selector:
    app.kubernetes.io/component: kafka-exporter
    {{- include "techbleat.selectorLabels" . | nindent 4 }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kafka-exporter
  namespace: {{ include "techbleat.namespace" . }}
  labels:
    release: {{ .Values.monitoring.prometheusReleaseLabel | quote }}
    {{- include "techbleat.labels" . | nindent 4 }}
    tier: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "techbleat.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: kafka-exporter
  namespaceSelector:
    matchNames:
      - {{ include "techbleat.namespace" . }}
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
{{- end }}
